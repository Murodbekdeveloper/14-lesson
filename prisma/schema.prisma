generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}
 
enum Role {
  admin
  user
}
model User {
  id Int @id @default(autoincrement())
  username String 
  password String   
  role Role @default(user)
  @@map("users")
}